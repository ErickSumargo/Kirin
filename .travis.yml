language: android
dist: trusty
android:
  components:
    - tools
    - platform-tools

    - extra-google-google_play_services
    - extra-google-m2repository
    - extra-android-m2repository

before_install:
  - yes | sdkmanager "platforms;android-29"
  - yes | sdkmanager "build-tools;29.0.3"

script:
  - ./gradlew clean build -Pbuild=ciDebug
